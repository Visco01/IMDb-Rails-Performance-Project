<%= form_with(model: title_crew) do |form| %>
  <% if title_crew.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(title_crew.errors.count, "error") %> prohibited this title_crew from being saved:</h2>

      <ul>
        <% title_crew.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tconst, style: "display: block" %>
    <%= form.number_field :tconst %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
